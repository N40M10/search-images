import './index.scss'

const Navbar = () => {
  const view = `
  <header class="navbar">
    <nav>
      <menu>
        <a href="#images">Images</a>
        <a href="#videos">Videos</a>
      </menu>
    </nav>
  </header>
  `
  return view
}

export default Navbar
